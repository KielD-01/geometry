const circle = require('./src/figures/circle');
const lines = require('./src/figures/lines');
const rectangle = require('./src/figures/rectangle');
const square = require('./src/figures/square');
const triangle = require('./src/figures/triangle');
const pyramid = require('./src/figures/pyramid');
const cone = require('./src/figures/cone');
const crescent = require('./src/figures/crescent');
const cube = require('./src/figures/cube');
const cuboid = require('./src/figures/cuboid');
const decagon = require('./src/figures/decagon');
const dodecahedron = require('./src/figures/dodecahedron');
const ellipse = require('./src/figures/ellipse');
const ellipsoid = require('./src/figures/ellipsoid');
const heptagon = require('./src/figures/heptagon');
const hexagon = require('./src/figures/hexagon');
const hexagonal_prism = require('./src/figures/hexagonal_prism');
const hexagonal_pyramid = require('./src/figures/hexagonal_pyramid');
const icosahedron = require('./src/figures/icosahedron');
const kite = require('./src/figures/kite');
const nonagon = require('./src/figures/nonagon');
const octagon = require('./src/figures/octagon');
const octahedron = require('./src/figures/octahedron');
const parallelogram = require('./src/figures/parallelogram');
const pentagon = require('./src/figures/pentagon');
const quadrilateral = require('./src/figures/quadrilateral');
const rhombus = require('./src/figures/rhombus');
const sphere = require('./src/figures/sphere');
const square_based_pyramid = require('./src/figures/square_based_pyramid');
const tetrahedron = require('./src/figures/tetrahedron');
const trapezium = require('./src/figures/trapezium');
const trapezoid = require('./src/figures/trapezoid');
const triangular_based_pyramid = require('./src/figures/triangular_based_pyramid');
const triangular_prism = require('./src/figures/triangular_prism');

module.exports = {
    cone,
    circle,
    crescent,
    cube,
    cuboid,
    decagon,
    dodecahedron,
    ellipse,
    ellipsoid,
    heptagon,
    hexagon,
    hexagonal_prism,
    hexagonal_pyramid,
    icosahedron,
    kite,
    lines,
    nonagon,
    octagon,
    octahedron,
    parallelogram,
    pentagon,
    pyramid,
    quadrilateral,
    rectangle,
    rhombus,
    sphere,
    square,
    square_based_pyramid,
    tetrahedron,
    trapezium,
    trapezoid,
    triangle,
    triangular_based_pyramid,
    triangular_prism
};