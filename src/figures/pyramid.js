/**
 * Dummy function to test the functionality
 *
 * @param text
 * @returns {string}
 */
const dummyFunction = function (text) {
    return `Hello ${text}!`;
};

/**
 * Function getPyramid for the pyramid
 *
 */
const getPyramid = function () {
    // ToDo : Describe the function body
};

module.exports = {
    dummyFunction,
    getPyramid
};