/**
 * Dummy function to test the functionality
 *
 * @param text
 * @returns {string}
 */
const dummyFunction = function (text) {
    return `Hello ${text}!`;
};

/**
 * Function getRectangle for the rectangle
 *
 */
const getRectangle = function () {
    // ToDo : Describe the function body
};

/**
 * Function getRectanglePerimeter for the rectangle
 *
 */
const getRectanglePerimeter = function () {
    // ToDo : Describe the function body
};

/**
 * Function getRectangleArea for the rectangle
 *
 */
const getRectangleArea = function () {
    // ToDo : Describe the function body
};

module.exports = {
    dummyFunction,
    getRectangle,
	getRectanglePerimeter,
	getRectangleArea
};