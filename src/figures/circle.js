/**
 * Dummy function to test the functionality
 *
 * @param text
 * @returns {string}
 */
const dummyFunction = function (text) {
    return `Hello ${text}!`;
};

/**
 * Function getCircle for the circle
 *
 */
const getCircle = function () {
    // ToDo : Describe the function body
};

/**
 * Function getRadius for the circle
 *
 */
const getRadius = function () {
    // ToDo : Describe the function body
};

module.exports = {
    dummyFunction,
    getCircle,
	getRadius
};